# Expected failures for the frozen legacy conformance job only.
#
# The current 2026-07-28 requirements job has no expected-failure baseline.
# These scenarios cover protocol features that 2026-07-28 deprecated or
# replaced. Keep this file paired with conformance CLI 0.1.16 and the
# APPLE_AGENT_MCP_CONFORMANCE_MODE=legacy server mode in ci.yml.
server:
  - logging-set-level          # logging/setLevel removed in 2026-07-28
  - tools-call-with-logging    # server logging deprecated; stderr/OTel instead
  - tools-call-sampling        # sampling deprecated (SEP-2577)
  - tools-call-elicitation     # server-initiated elicitation replaced by MRTR
  - elicitation-sep1034-defaults
  - elicitation-sep1330-enums
  - resources-subscribe        # replaced by subscriptions/listen
  - resources-unsubscribe
